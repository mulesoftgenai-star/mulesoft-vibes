# Payment System API Configuration
http.host=0.0.0.0
http.port=8085

# Payment Gateway Configuration
payment.gateway.url=${secure::payment.gateway.url}
payment.gateway.api.key=${secure::payment.gateway.api.key}
payment.gateway.merchant.id=${secure::payment.gateway.merchant.id}

# API Metadata
api.name=Payment System API
api.version=v1.0.0
api.description=System layer API for payment processing and transaction management