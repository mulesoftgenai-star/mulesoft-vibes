<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core" 
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:http="http://www.mulesoft.org/schema/mule/http" 
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd 
                          http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <!-- Configuration Properties -->
    <configuration-properties file="config.properties" doc:name="Configuration properties"/>

    <!-- HTTP Listener Configuration -->
    <http:listener-config name="HTTP_Listener_config" doc:name="HTTP Listener config">
        <http:listener-connection host="${http.host}" port="${http.port}"/>
    </http:listener-config>

    <!-- Customer System API HTTP Request Configuration -->
    <http:request-config name="Customer_System_API_Config" doc:name="Customer System API Config">
        <http:request-connection host="${api.customer.host}" port="${api.customer.port}" protocol="${api.customer.protocol}">
            <http:request-builder>
                <http:header headerName="Accept" value="application/json"/>
                <http:header headerName="Content-Type" value="application/json"/>
            </http:request-builder>
        </http:request-connection>
    </http:request-config>

    <!-- Inventory System API HTTP Request Configuration -->
    <http:request-config name="Inventory_System_API_Config" doc:name="Inventory System API Config">
        <http:request-connection host="${api.inventory.host}" port="${api.inventory.port}" protocol="${api.inventory.protocol}">
            <http:request-builder>
                <http:header headerName="Accept" value="application/json"/>
                <http:header headerName="Content-Type" value="application/json"/>
            </http:request-builder>
        </http:request-connection>
    </http:request-config>

    <!-- Payment System API HTTP Request Configuration -->
    <http:request-config name="Payment_System_API_Config" doc:name="Payment System API Config">
        <http:request-connection host="${api.payment.host}" port="${api.payment.port}" protocol="${api.payment.protocol}">
            <http:request-builder>
                <http:header headerName="Accept" value="application/json"/>
                <http:header headerName="Content-Type" value="application/json"/>
            </http:request-builder>
        </http:request-connection>
    </http:request-config>

    <!-- Shipping System API HTTP Request Configuration -->
    <http:request-config name="Shipping_System_API_Config" doc:name="Shipping System API Config">
        <http:request-connection host="${api.shipping.host}" port="${api.shipping.port}" protocol="${api.shipping.protocol}">
            <http:request-builder>
                <http:header headerName="Accept" value="application/json"/>
                <http:header headerName="Content-Type" value="application/json"/>
            </http:request-builder>
        </http:request-connection>
    </http:request-config>

</mule>